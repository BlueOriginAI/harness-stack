# Workflow Rules

`workflows/*.md` 是 harness-stack 的主要流程承载层。

## 原则

1. workflow 要厚
2. 明确输入、步骤、输出、结束条件
3. 保持结构化，方便 skill 模板复用
4. 尽量用清楚的小节，而不是一整段散文

## 建议结构

- 目的
- 何时使用
- 输入
- 流程步骤
- 输出
- 结束条件

## 约束

- 每个 workflow 只服务一个核心命令
- 不在 workflow 里加入与当前能力无关的旁支能力
- 第一版保持克制，不引入重型状态管理
